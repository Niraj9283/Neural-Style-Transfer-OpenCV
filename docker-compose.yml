services:
  neural-style-transfer:
    build: .
    container_name: neural-style-transfer-opencv
    ports:
      - "5000:5000"
    volumes:
      - ./outputs:/app/outputs
      - ./uploads:/app/uploads
    restart: unless-stopped
