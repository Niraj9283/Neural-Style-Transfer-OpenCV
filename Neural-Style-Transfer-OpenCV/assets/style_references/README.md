# Style References

The project uses fixed pretrained style models, so a separate style image is not required at runtime.

This folder is kept for presentation material. You can place reference images or screenshots here if you want to show which artistic style each model represents.
