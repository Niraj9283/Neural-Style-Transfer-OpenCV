# Assets

The `sample_content` folder contains simple generated images for testing the project without depending on external image files.

You can add your own `.jpg` or `.png` files to `assets/sample_content` and select them in the Flask web app.
